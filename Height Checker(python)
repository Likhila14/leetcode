class Solution:
    def heightChecker(self, heights: List[int]) -> int:
        ans = 0;
        s = sorted( heights)
        for i in range(0,len(heights)):
            if(heights[i]!= s[i]):
                ans+=1
        return ans
